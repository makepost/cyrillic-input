{
  "name": "cyrillic-input",
  "version": "1.0.0",
  "description": "Regular expressions for autocomplete, to match user input against",
  "main": "CyrillicInput",
  "types": "CyrillicInput",
  "scripts": {
    "format": "tsc -p . --noEmit && tslint --fix '*.js'",
    "prepare": "tsc -p ."
  },
  "repository": "https://github.com/makepost/cyrillic-input",
  "author": "makepost <makepost@firemail.cc>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.11.1",
    "tslint": "^5.9.1",
    "tslint-config-prettier": "^1.10.0",
    "tslint-plugin-prettier": "^1.3.0",
    "typescript": "^2.8.1"
  }
}
